import { DSStateValueSelf } from "dependingState";

export class __Name__Value extends DSStateValueSelf<__Name__Value> {
    constructor() {
        super();
    }
}

/*
import { DSStateValue } from "dependingState";

export type __Name__Value = {
    foo: string;
    bar: string;
}
export type __Name__StateValue = DSStateValue<__Name__Value>;
*/

/*
import { DSStateValue } from "dependingState";

export class TimesheetValue {
    constructor(
        public propA: string,
        public propB: string
    ){        
    }
}

// export type TimesheetStateValue = DSStateValue<TimesheetValue>;
*/
